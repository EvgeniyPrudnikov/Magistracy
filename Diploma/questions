Вопросы:
	
	1. можем ли мы исходя из диаграммы заполненности удалить колонки (например там где % заполненности <= 15 %)?
	2. или лучше использовать PCA (principal components)
	3. есть ли смысл приводить колонки к int, тоесть * на 1000 ?
	4. что делать с несбалансированным датасетом? можно ли выбрать все label = 1 (~ 6000 ) и семплы по (label = 0) и обучать несколько моделей и потом голосовать ? 
 	???4.удаляем колонки по numeric dataset, в остальных удаляем соответсвующие колонки ? по категориям скорее всего нет так как там очень много пустых колонок
